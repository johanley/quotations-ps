/** 
 Input from a structured text file.
 Parse into a sorted list of quotation objects.
 
 The quotation objects have a toString() which 
 can be used as a default formatting.
*/
 package quote.parser;